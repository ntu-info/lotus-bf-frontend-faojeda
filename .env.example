# Example env file for local development
# Set the backend base URL for the API (default in code is the production server)
VITE_API_BASE=https://mil.psy.ntu.edu.tw:5000

# To run locally against a different backend, copy this file to .env and edit the URL.
